// Client-side mirror of supabase/functions/_shared/sector-aliases.ts.
// Kept in sync so UI tooltips can show the same canonical resolution.
// If you change this file, also update the edge-function copy.
export {
  ALIASES,
  slugify,
  resolveSectorCanonical,
} from "../../supabase/functions/_shared/sector-aliases.ts";
